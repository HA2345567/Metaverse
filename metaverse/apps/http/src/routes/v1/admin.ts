import {Router} from "express";

export const adminRouter = Router();

adminRouter.get("/api/v1/space/:spaceId", (res,req) =>{
    
})

adminRouter.get(":/elementId", (res,req) =>{

})

adminRouter.get("/element/:elementId", (res,req) =>{

})

adminRouter.get("/avatar", (res,req) =>{

})

adminRouter.get("/map", (res,req) =>{

})

